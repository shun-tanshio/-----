使い方

1. 仮想環境を有効化し、依存をインストールします:

```bash
pip install -r requirements.txt
```

2. Streamlit アプリを起動します (修正版):

```bash
streamlit run Streamlit_app.py
```

3. 画面の左サイドバーでティッカーと2つの期間を指定し、グラフとシミュレーション結果を確認してください。

備考: `prices_close_wide.csv` はこのファイルの1階層上に置かれている必要があります。
